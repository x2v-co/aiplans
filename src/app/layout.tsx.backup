import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "PlanPrice.ai - Compare AI Pricing & Save Money",
  description: "Compare AI model pricing across providers and find the best deals.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
